/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
import { IDatabaseManager, IDocumentStorage, IWebSocketServer, ILogger, IDocumentRepository, ICheckpointRepository, CheckpointService } from "@fluidframework/server-services-core";
import { IConcreteNodeFactory } from "./interfaces";
import { LocalNode } from "./localNode";
/**
 * @internal
 */
export declare class LocalNodeFactory implements IConcreteNodeFactory {
    private readonly hostname;
    private readonly address;
    private readonly storage;
    private readonly databaseManager;
    private readonly documentRepository;
    private readonly deliCheckpointRepository;
    private readonly scribeCheckpointRepository;
    private readonly deliCheckpointService;
    private readonly scribeCheckpointService;
    private readonly timeoutLength;
    private readonly webSocketServerFactory;
    private readonly maxMessageSize;
    private readonly logger;
    constructor(hostname: string, address: string, storage: IDocumentStorage, databaseManager: IDatabaseManager, documentRepository: IDocumentRepository, deliCheckpointRepository: ICheckpointRepository, scribeCheckpointRepository: ICheckpointRepository, deliCheckpointService: CheckpointService, scribeCheckpointService: CheckpointService, timeoutLength: number, webSocketServerFactory: () => IWebSocketServer, maxMessageSize: number, logger: ILogger);
    create(): Promise<LocalNode>;
}
//# sourceMappingURL=localNodeFactory.d.ts.map