/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
import { IClient, IDocumentMessage } from "@fluidframework/protocol-definitions";
import { IDocument, IOrdererConnection, IProducer, IServiceConfiguration } from "@fluidframework/server-services-core";
import { ISubscriber } from "./pubsub";
export declare class LocalOrdererConnection implements IOrdererConnection {
    socket: ISubscriber;
    readonly existing: boolean;
    private readonly producer;
    readonly tenantId: string;
    readonly documentId: string;
    readonly clientId: string;
    private readonly client;
    readonly serviceConfiguration: IServiceConfiguration;
    readonly maxMessageSize: number;
    constructor(socket: ISubscriber, existing: boolean, document: IDocument, producer: IProducer, tenantId: string, documentId: string, clientId: string, client: IClient, serviceConfiguration: IServiceConfiguration);
    connect(clientJoinMessageServerMetadata?: any): Promise<void>;
    order(messages: IDocumentMessage[]): Promise<void>;
    disconnect(): Promise<void>;
    once(event: "error", listener: (...args: any[]) => void): void;
    off(event: "error", listener: (...args: any[]) => void): void;
    private submitRawOperation;
}
//# sourceMappingURL=localOrdererConnection.d.ts.map