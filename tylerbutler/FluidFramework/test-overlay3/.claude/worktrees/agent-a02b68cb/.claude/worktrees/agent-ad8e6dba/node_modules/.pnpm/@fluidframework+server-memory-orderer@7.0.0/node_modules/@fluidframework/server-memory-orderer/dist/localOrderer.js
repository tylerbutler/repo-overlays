"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LocalOrderer = void 0;
const protocol_base_1 = require("@fluidframework/protocol-base");
const server_lambdas_1 = require("@fluidframework/server-lambdas");
const server_services_client_1 = require("@fluidframework/server-services-client");
const server_services_core_1 = require("@fluidframework/server-services-core");
const server_services_telemetry_1 = require("@fluidframework/server-services-telemetry");
const lodash_1 = require("lodash");
const localContext_1 = require("./localContext");
const localKafka_1 = require("./localKafka");
const localLambdaController_1 = require("./localLambdaController");
const localOrdererConnection_1 = require("./localOrdererConnection");
const localOrdererSetup_1 = require("./localOrdererSetup");
const pubsub_1 = require("./pubsub");
const DefaultScribe = {
    lastClientSummaryHead: undefined,
    logOffset: -1,
    minimumSequenceNumber: -1,
    protocolState: {
        members: [],
        minimumSequenceNumber: 0,
        proposals: [],
        sequenceNumber: 0,
        values: [],
    },
    sequenceNumber: -1,
    lastSummarySequenceNumber: 0,
    validParentSummaries: undefined,
    isCorrupt: false,
    protocolHead: undefined,
    checkpointTimestamp: Date.now(),
};
const DefaultDeli = {
    clients: undefined,
    durableSequenceNumber: 0,
    expHash1: server_services_client_1.defaultHash,
    logOffset: -1,
    sequenceNumber: 0,
    signalClientConnectionNumber: 0,
    lastSentMSN: 0,
    nackMessages: undefined,
    checkpointTimestamp: undefined,
};
class LocalSocketPublisher {
    constructor(publisher) {
        this.publisher = publisher;
    }
    on(event, listener) {
        return;
    }
    to(topic) {
        return {
            emit: (event, ...args) => this.publisher.publish(topic, event, ...args),
        };
    }
    async close() { }
}
/**
 * Performs local ordering of messages based on an in-memory stream of operations.
 * @internal
 */
class LocalOrderer {
    static async load(storage, databaseManager, tenantId, documentId, logger, documentRepository, deliCheckpointRepository, scribeCheckpointRepository, deliCheckpointService, scribeCheckpointService, gitManager, setup = new localOrdererSetup_1.LocalOrdererSetup(tenantId, documentId, storage, databaseManager, documentRepository, deliCheckpointRepository, scribeCheckpointRepository, deliCheckpointService, scribeCheckpointService, gitManager), pubSub = new pubsub_1.PubSub(), broadcasterContext = new localContext_1.LocalContext(logger), scriptoriumContext = new localContext_1.LocalContext(logger), scribeContext = new localContext_1.LocalContext(logger), deliContext = new localContext_1.LocalContext(logger), moiraContext = new localContext_1.LocalContext(logger), serviceConfiguration = {}) {
        const documentDetails = await setup.documentP();
        return new LocalOrderer(setup, documentDetails, tenantId, documentId, gitManager, pubSub, broadcasterContext, scriptoriumContext, scribeContext, deliContext, moiraContext, (0, lodash_1.merge)({}, server_services_core_1.DefaultServiceConfiguration, serviceConfiguration));
    }
    constructor(setup, details, tenantId, documentId, gitManager, pubSub, broadcasterContext, scriptoriumContext, scribeContext, deliContext, moiraContext, serviceConfiguration) {
        this.setup = setup;
        this.details = details;
        this.tenantId = tenantId;
        this.documentId = documentId;
        this.gitManager = gitManager;
        this.pubSub = pubSub;
        this.broadcasterContext = broadcasterContext;
        this.scriptoriumContext = scriptoriumContext;
        this.scribeContext = scribeContext;
        this.deliContext = deliContext;
        this.moiraContext = moiraContext;
        this.serviceConfiguration = serviceConfiguration;
        this.existing = details.existing;
        this.dbObject = this.getDeliState();
        this.socketPublisher = new LocalSocketPublisher(this.pubSub);
        this.setupKafkas();
        this.setupLambdas();
        this.startLambdas();
    }
    async connect(socket, clientId, client) {
        const socketSubscriber = new pubsub_1.WebSocketSubscriber(socket);
        const orderer = this.connectInternal(socketSubscriber, clientId, client);
        return orderer;
    }
    connectInternal(subscriber, clientId, client) {
        // Create the connection
        const connection = new localOrdererConnection_1.LocalOrdererConnection(subscriber, this.existing, this.details.value, this.rawDeltasKafka, this.tenantId, this.documentId, clientId, client, this.serviceConfiguration);
        // Document is now existing regardless of the original value
        this.existing = true;
        return connection;
    }
    async close() {
        await this.closeKafkas();
        this.closeLambdas();
    }
    hasPendingWork() {
        if (this.broadcasterLambda?.lambda) {
            return this.broadcasterLambda.lambda.hasPendingWork();
        }
        return false;
    }
    setupKafkas() {
        const deliState = JSON.parse(this.dbObject.deli);
        this.rawDeltasKafka = new localKafka_1.LocalKafka(deliState.logOffset + 1);
        this.deltasKafka = new localKafka_1.LocalKafka();
    }
    setupLambdas() {
        this.scriptoriumLambda = new localLambdaController_1.LocalLambdaController(this.deltasKafka, this.setup, this.scriptoriumContext, async (lambdaSetup, context) => {
            const deltasCollection = await lambdaSetup.deltaCollectionP();
            return new server_lambdas_1.ScriptoriumLambda(deltasCollection, context, undefined, async () => Promise.resolve());
        });
        this.broadcasterLambda = new localLambdaController_1.LocalLambdaController(this.deltasKafka, this.setup, this.broadcasterContext, async (_, context) => new server_lambdas_1.BroadcasterLambda(this.socketPublisher, context, this.serviceConfiguration, undefined));
        if (this.gitManager) {
            this.scribeLambda = new localLambdaController_1.LocalLambdaController(this.deltasKafka, this.setup, this.scribeContext, 
            // eslint-disable-next-line @typescript-eslint/promise-function-async
            (lambdaSetup, context) => this.startScribeLambda(lambdaSetup, context));
        }
        this.deliLambda = new localLambdaController_1.LocalLambdaController(this.rawDeltasKafka, this.setup, this.deliContext, async (lambdaSetup, context) => {
            const checkpointService = await lambdaSetup.checkpointServiceP("deli");
            const lastCheckpoint = JSON.parse(this.dbObject.deli);
            const checkpointManager = (0, server_lambdas_1.createDeliCheckpointManagerFromCollection)(this.tenantId, this.documentId, checkpointService);
            return new server_lambdas_1.DeliLambda(context, this.tenantId, this.documentId, lastCheckpoint, checkpointManager, undefined, this.deltasKafka, undefined, this.rawDeltasKafka, this.serviceConfiguration, undefined, checkpointService);
        });
        if (this.serviceConfiguration.moira.enable) {
            this.moiraLambda = new localLambdaController_1.LocalLambdaController(this.deltasKafka, this.setup, this.moiraContext, async (_, context) => new server_lambdas_1.MoiraLambda(context, this.serviceConfiguration, this.tenantId, this.documentId));
        }
    }
    async startScribeLambda(setup, context) {
        // Scribe lambda
        const [documentRepository, localCheckpointCollection, scribeMessagesCollection, protocolHead, scribeMessages,] = await Promise.all([
            setup.documentRepositoryP(),
            setup.scribeCheckpointRepositoryP(),
            setup.scribeDeltaCollectionP(),
            setup.protocolHeadP(),
            setup.scribeMessagesP(),
        ]);
        const scribe = this.getScribeState();
        const lastState = scribe.protocolState
            ? scribe.protocolState
            : { members: [], proposals: [], values: [] };
        const protocolHandler = new protocol_base_1.ProtocolOpHandler(scribe.minimumSequenceNumber, scribe.sequenceNumber, lastState.members, lastState.proposals, lastState.values, () => -1);
        if (!this.gitManager) {
            throw new Error("Git manager is required to start scribe lambda.");
        }
        const summaryReader = new server_lambdas_1.SummaryReader(this.tenantId, this.documentId, this.gitManager, false, this.details.value.isEphemeralContainer);
        const latestSummary = await summaryReader.readLastSummary();
        const summaryWriter = new server_lambdas_1.SummaryWriter(this.tenantId, this.documentId, this.gitManager, undefined /* deltaService */, scribeMessagesCollection, false /* enableWholeSummaryUpload */, latestSummary.messages, false /* getDeltasViaAlfred */);
        const checkpointService = new server_services_core_1.CheckpointService(localCheckpointCollection, documentRepository, false);
        const checkpointManager = new server_lambdas_1.CheckpointManager(context, this.tenantId, this.documentId, documentRepository, scribeMessagesCollection, undefined /* deltaService */, false /* getDeltasViaAlfred */, false /* verifyLastOpPersistence */, checkpointService);
        const maxPendingCheckpointMessagesLength = 2000;
        return new server_lambdas_1.ScribeLambda(context, this.tenantId, this.documentId, summaryWriter, undefined, checkpointManager, scribe, this.serviceConfiguration, this.rawDeltasKafka, protocolHandler, protocolHead, scribeMessages.map((message) => message.operation), undefined, new Set(), true, true, true, this.details.value.isEphemeralContainer ?? false, checkpointService.getLocalCheckpointEnabled(), maxPendingCheckpointMessagesLength);
    }
    startLambdas() {
        const lumberjackProperties = {
            ...(0, server_services_telemetry_1.getLumberBaseProperties)(this.documentId, this.tenantId),
        };
        if (this.deliLambda) {
            this.deliLambda.start().catch((err) => {
                server_services_telemetry_1.Lumberjack.error("Error starting memory orderer deli lambda", lumberjackProperties, err);
            });
        }
        if (this.scriptoriumLambda) {
            this.scriptoriumLambda.start().catch((err) => {
                server_services_telemetry_1.Lumberjack.error("Error starting memory orderer scriptorium lambda", lumberjackProperties, err);
            });
        }
        if (this.scribeLambda) {
            this.scribeLambda.start().catch((err) => {
                server_services_telemetry_1.Lumberjack.error("Error starting memory orderer scribe lambda", lumberjackProperties, err);
            });
        }
        if (this.broadcasterLambda) {
            this.broadcasterLambda.start().catch((err) => {
                server_services_telemetry_1.Lumberjack.error("Error starting memory orderer broadcaster lambda", lumberjackProperties, err);
            });
        }
        if (this.moiraLambda) {
            this.moiraLambda.start().catch((err) => {
                server_services_telemetry_1.Lumberjack.error("Error starting memory orderer moira lambda", lumberjackProperties, err);
            });
        }
    }
    async closeKafkas() {
        await Promise.all([this.rawDeltasKafka.close(), this.deltasKafka.close()]);
    }
    closeLambdas() {
        if (this.deliLambda) {
            this.deliLambda.close();
            this.deliLambda = undefined;
        }
        if (this.scriptoriumLambda) {
            this.scriptoriumLambda.close();
            this.scriptoriumLambda = undefined;
        }
        if (this.scribeLambda) {
            this.scribeLambda.close();
            this.scribeLambda = undefined;
        }
        if (this.broadcasterLambda) {
            this.broadcasterLambda.close();
            this.broadcasterLambda = undefined;
        }
        if (this.moiraLambda) {
            this.moiraLambda.close();
            this.moiraLambda = undefined;
        }
    }
    getDeliState() {
        const dbObject = this.details.value;
        if (dbObject.deli === undefined || dbObject.deli === null) {
            dbObject.deli = JSON.stringify(DefaultDeli);
        }
        return dbObject;
    }
    getScribeState() {
        const dbObject = this.details.value;
        const scribe = dbObject.scribe === undefined || dbObject.scribe === null
            ? DefaultScribe
            : typeof this.details.value.scribe === "string"
                ? JSON.parse(this.details.value.scribe)
                : this.details.value.scribe;
        return scribe;
    }
}
exports.LocalOrderer = LocalOrderer;
//# sourceMappingURL=localOrderer.js.map