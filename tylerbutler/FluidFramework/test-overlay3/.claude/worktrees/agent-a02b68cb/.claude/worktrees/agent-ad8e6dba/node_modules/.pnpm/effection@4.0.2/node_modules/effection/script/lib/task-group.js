"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TaskGroupContext = exports.TaskGroup = void 0;
exports.encapsulate = encapsulate;
const context_js_1 = require("./context.js");
const box_js_1 = require("./box.js");
const result_js_1 = require("./result.js");
class TaskGroup {
    constructor() {
        Object.defineProperty(this, "tasks", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: new Set()
        });
    }
    add(task) {
        this.tasks.add(task);
    }
    delete(task) {
        this.tasks.delete(task);
    }
    *halt() {
        let total = (0, result_js_1.Ok)();
        while (this.tasks.size > 0) {
            let tasks = [...this.tasks].reverse();
            this.tasks.clear();
            for (let task of tasks) {
                let result = yield* (0, box_js_1.box)(task.halt);
                if (!result.ok) {
                    total = result;
                }
            }
        }
        (0, result_js_1.unbox)(total);
    }
}
exports.TaskGroup = TaskGroup;
exports.TaskGroupContext = (0, context_js_1.createContext)("@effection/task-group", new TaskGroup());
function encapsulate(operation) {
    return exports.TaskGroupContext.with(new TaskGroup(), function* (group) {
        try {
            return yield* operation();
        }
        finally {
            yield* group.halt();
        }
    });
}
//# sourceMappingURL=task-group.js.map