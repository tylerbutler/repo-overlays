/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
/// <reference types="node" />
import { EventEmitter } from "events";
import { IContext, IQueuedMessage, IPartitionLambda } from "@fluidframework/server-services-core";
import { IKafkaSubscriber, ILocalOrdererSetup } from "./interfaces";
import { LocalKafka } from "./localKafka";
/**
 * @internal
 */
export type LocalLambdaControllerState = "created" | "starting" | "started" | "closed";
/**
 * Controls lambda startups and subscriptions for localOrderer
 * @internal
 */
export declare class LocalLambdaController<T = ILocalOrdererSetup> extends EventEmitter implements IKafkaSubscriber {
    private readonly kafaka;
    private readonly setup;
    readonly context: IContext;
    private readonly starter;
    lambda: IPartitionLambda | undefined;
    private _state;
    private startTimer;
    constructor(kafaka: LocalKafka, setup: T, context: IContext, starter: (setup: T, context: IContext) => Promise<IPartitionLambda>);
    get state(): LocalLambdaControllerState;
    start(): Promise<void>;
    close(): void;
    process(message: IQueuedMessage): Promise<void> | undefined;
}
//# sourceMappingURL=localLambdaController.d.ts.map