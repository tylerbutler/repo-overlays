"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.createNewContainerOnExistingFile = void 0;
const internal_1 = require("@fluidframework/driver-utils/internal");
const internal_2 = require("@fluidframework/telemetry-utils/internal");
const contractsPublic_js_1 = require("./../contractsPublic.js");
const createOdspUrl_js_1 = require("./../createOdspUrl.js");
const odspDriverUrlResolver_js_1 = require("./../odspDriverUrlResolver.js");
const odspUrlHelper_js_1 = require("./../odspUrlHelper.js");
const odspUtils_js_1 = require("./../odspUtils.js");
const createNewUtils_js_1 = require("./createNewUtils.js");
/**
 * Creates a new Fluid container on an existing file.
 *
 * @remarks This requires service's capability to manage Fluid container inside an existing file.
 *
 * @example
 *
 * This enables a scenario where Fluid data is not stored as a standalone file but in a way that is managed
 * by an existing file. For example, SharePoint Pages is able to store Fluid container in an
 * "alternative file partition" where the main File stub is an ASPX page.
 */
async function createNewContainerOnExistingFile(getAuthHeader, fileInfo, logger, createNewSummary, epochTracker, fileEntry, createNewCaching, forceAccessTokenViaAuthorizationHeader, isClpCompliantApp, eTag) {
    if (createNewSummary === undefined) {
        throw new internal_1.UsageError("createNewSummary must exist to create a new container");
    }
    const baseUrl = `${(0, odspUrlHelper_js_1.getApiRoot)(new URL(fileInfo.siteUrl))}/drives/${fileInfo.driveId}/items/${fileInfo.itemId}`;
    const containerSnapshot = (0, createNewUtils_js_1.convertSummaryIntoContainerSnapshot)(createNewSummary);
    const initialUrl = `${baseUrl}/opStream/snapshots/snapshot`;
    const additionalHeaders = {};
    if (eTag !== undefined) {
        // Sending the e-tag of the file in the If-Match triggers file conversion logic in the /snapshot api.
        additionalHeaders["If-Match"] = eTag;
    }
    const { id: summaryHandle } = await (0, createNewUtils_js_1.createNewFluidContainerCore)({
        containerSnapshot,
        getAuthHeader,
        logger,
        initialUrl,
        forceAccessTokenViaAuthorizationHeader,
        epochTracker,
        telemetryName: "CreateNewContainerOnExistingFile",
        fetchType: "uploadSummary",
        additionalHeaders,
    });
    const odspUrl = (0, createOdspUrl_js_1.createOdspUrl)({ ...fileInfo, dataStorePath: "/" });
    const resolver = new odspDriverUrlResolver_js_1.OdspDriverUrlResolver();
    const odspResolvedUrl = await resolver.resolve({
        url: odspUrl,
        headers: {
            [contractsPublic_js_1.ClpCompliantAppHeader.isClpCompliantApp]: isClpCompliantApp,
            [contractsPublic_js_1.FileMetadataHeader.eTag]: eTag,
        },
    });
    fileEntry.docId = odspResolvedUrl.hashedDocumentId;
    fileEntry.resolvedUrl = odspResolvedUrl;
    if (createNewCaching) {
        // converting summary and getting sequence number
        const snapshot = (0, createNewUtils_js_1.convertCreateNewSummaryTreeToTreeAndBlobs)(createNewSummary, summaryHandle);
        // caching the converted summary
        await epochTracker.put((0, odspUtils_js_1.createCacheSnapshotKey)(odspResolvedUrl, (0, odspUtils_js_1.snapshotWithLoadingGroupIdSupported)((0, internal_2.loggerToMonitoringContext)(logger).config)), snapshot);
    }
    return odspResolvedUrl;
}
exports.createNewContainerOnExistingFile = createNewContainerOnExistingFile;
//# sourceMappingURL=createNewContainerOnExistingFile.js.map