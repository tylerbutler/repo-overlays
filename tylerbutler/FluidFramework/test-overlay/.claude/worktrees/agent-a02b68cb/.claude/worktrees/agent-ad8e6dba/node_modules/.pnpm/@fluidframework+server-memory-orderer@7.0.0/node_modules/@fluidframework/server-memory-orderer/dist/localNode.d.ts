/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
/// <reference types="node" />
import { EventEmitter } from "events";
import { IDatabaseManager, IDocumentStorage, IOrderer, IWebSocketServer, ILogger, IDocumentRepository, ICheckpointRepository, CheckpointService } from "@fluidframework/server-services-core";
import { IConcreteNode } from "./interfaces";
export declare class LocalNode extends EventEmitter implements IConcreteNode {
    private readonly webSocketServerFactory;
    private node;
    private readonly storage;
    private readonly databaseManager;
    private readonly documentRepository;
    private readonly deliCheckpointRepository;
    private readonly scribeCheckpointRepository;
    private readonly deliCheckpointService;
    private readonly scribeCheckpointService;
    private readonly timeoutLength;
    private readonly maxMessageSize;
    private readonly logger;
    static connect(id: string, address: string, storage: IDocumentStorage, databaseManager: IDatabaseManager, documentRepository: IDocumentRepository, deliCheckpointRepository: ICheckpointRepository, scribeCheckpointRepository: ICheckpointRepository, deliCheckpointService: CheckpointService, scribeCheckpointService: CheckpointService, timeoutLength: number, webSocketServerFactory: () => IWebSocketServer, maxMessageSize: number, logger: ILogger): Promise<LocalNode>;
    private static create;
    private static updateExpiration;
    get id(): string;
    readonly valid = true;
    private readonly webSocketServer;
    private readonly orderMap;
    private readonly connectionMap;
    private constructor();
    connectOrderer(tenantId: string, documentId: string): Promise<IOrderer>;
    private scheduleHeartbeat;
}
//# sourceMappingURL=localNode.d.ts.map