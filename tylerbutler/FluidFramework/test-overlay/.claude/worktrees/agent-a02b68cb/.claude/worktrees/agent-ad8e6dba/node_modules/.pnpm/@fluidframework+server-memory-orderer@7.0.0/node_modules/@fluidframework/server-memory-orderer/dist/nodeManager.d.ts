/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
/// <reference types="node" />
import { EventEmitter } from "events";
import { MongoManager } from "@fluidframework/server-services-core";
import { IConcreteNode } from "./interfaces";
/**
 * Tracks the validity of a set of nodes.
 * @internal
 */
export declare class NodeManager extends EventEmitter {
    private readonly mongoManager;
    private readonly nodeCollectionName;
    private readonly nodes;
    private readonly pendingNodes;
    constructor(mongoManager: MongoManager, nodeCollectionName: string);
    /**
     * Registers a new local node with the NodeManager
     */
    registerLocal(node: IConcreteNode): void;
    /**
     * Loads the given remote node with the provided ID
     */
    loadRemote(id: string): Promise<IConcreteNode>;
    private getNode;
}
//# sourceMappingURL=nodeManager.d.ts.map