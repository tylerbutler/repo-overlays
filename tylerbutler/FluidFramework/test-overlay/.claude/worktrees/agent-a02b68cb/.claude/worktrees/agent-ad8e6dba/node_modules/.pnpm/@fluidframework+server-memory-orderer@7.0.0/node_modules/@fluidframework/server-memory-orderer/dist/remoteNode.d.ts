/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
/// <reference types="node" />
import { EventEmitter } from "events";
import { IClient } from "@fluidframework/protocol-definitions";
import { IOrderer, IOrdererConnection, IWebSocket, MongoManager } from "@fluidframework/server-services-core";
import { IConcreteNode } from "./interfaces";
/**
 * Connection to a remote node
 */
export declare class RemoteNode extends EventEmitter implements IConcreteNode {
    private readonly _id;
    private readonly socket;
    static connect(id: string, mongoManager: MongoManager, nodeCollectionName: string): Promise<RemoteNode>;
    get id(): string;
    get valid(): boolean;
    private readonly connectMap;
    private readonly orderers;
    private readonly topicMap;
    private cid;
    private constructor();
    connectOrderer(tenantId: string, documentId: string): Promise<IOrderer>;
    send(cid: number, type: string, payload: any): void;
    connect(socket: IWebSocket, tenantId: string, documentId: string, client: IClient): Promise<IOrdererConnection>;
    private route;
    private getNextCid;
}
//# sourceMappingURL=remoteNode.d.ts.map