"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.LocalNode = void 0;
const assert_1 = __importDefault(require("assert"));
const events_1 = require("events");
const server_services_core_1 = require("@fluidframework/server-services-core");
const server_services_telemetry_1 = require("@fluidframework/server-services-telemetry");
const _ = __importStar(require("lodash"));
const sillyname_1 = __importDefault(require("sillyname"));
const uuid_1 = require("uuid");
const debug_1 = require("./debug");
const localOrderer_1 = require("./localOrderer");
const socket_1 = require("./socket");
// Can I treat each Alfred as a mini-Kafka. And consolidate all the deli logic together?
// Rather than creating one per? I'm in some ways on this path.
class RemoteSubscriber {
    constructor(socket) {
        this.socket = socket;
        this.id = (0, uuid_1.v4)();
    }
    send(topic, event, ...args) {
        const opMessage = {
            data: args,
            op: event,
            topic,
        };
        const message = {
            cid: -1,
            payload: opMessage,
            type: "op",
        };
        this.socket.send(message);
    }
}
// Local node manages maintaining the reservation. As well as handling managing the local orderers.
// Messages sent to it are directly routed.
class LocalNode extends events_1.EventEmitter {
    static async connect(id, address, storage, databaseManager, documentRepository, deliCheckpointRepository, scribeCheckpointRepository, deliCheckpointService, scribeCheckpointService, timeoutLength, webSocketServerFactory, maxMessageSize, logger) {
        // Look up any existing information for the node or create a new one
        const node = await LocalNode.create(id, address, databaseManager, timeoutLength);
        return new LocalNode(webSocketServerFactory, node, storage, databaseManager, documentRepository, deliCheckpointRepository, scribeCheckpointRepository, deliCheckpointService, scribeCheckpointService, timeoutLength, maxMessageSize, logger);
    }
    static async create(id, address, databaseManager, timeoutLength) {
        (0, debug_1.debug)("Creating node", id);
        server_services_telemetry_1.Lumberjack.debug(`Creating node: ${id}`);
        const nodeCollection = await databaseManager.getNodeCollection();
        const node = {
            _id: id,
            address,
            expiration: Date.now() + timeoutLength,
        };
        await nodeCollection.insertOne(node);
        return node;
    }
    static async updateExpiration(existing, databaseManager, timeoutLength) {
        const nodeCollection = await databaseManager.getNodeCollection();
        const newExpiration = Date.now() + timeoutLength;
        await nodeCollection.update({
            _id: existing._id,
            expiration: existing.expiration,
        }, {
            expiration: newExpiration,
        }, null);
        // eslint-disable-next-line import/namespace
        const result = _.clone(existing);
        result.expiration = newExpiration;
        return result;
    }
    get id() {
        return this.node._id;
    }
    constructor(webSocketServerFactory, node, storage, databaseManager, documentRepository, deliCheckpointRepository, scribeCheckpointRepository, deliCheckpointService, scribeCheckpointService, timeoutLength, maxMessageSize, logger) {
        super();
        this.webSocketServerFactory = webSocketServerFactory;
        this.node = node;
        this.storage = storage;
        this.databaseManager = databaseManager;
        this.documentRepository = documentRepository;
        this.deliCheckpointRepository = deliCheckpointRepository;
        this.scribeCheckpointRepository = scribeCheckpointRepository;
        this.deliCheckpointService = deliCheckpointService;
        this.scribeCheckpointService = scribeCheckpointService;
        this.timeoutLength = timeoutLength;
        this.maxMessageSize = maxMessageSize;
        this.logger = logger;
        this.valid = true;
        this.orderMap = new Map();
        this.connectionMap = new Map();
        // Schedule the first heartbeat to update the reservation
        this.scheduleHeartbeat();
        // Start up the peer-to-peer socket server to listen to inbound messages
        this.webSocketServer = this.webSocketServerFactory();
        // Connections will arrive from remote nodes
        this.webSocketServer.on("connection", (wsSocket, request) => {
            (0, debug_1.debug)(`New inbound web socket connection ${request.url}`);
            server_services_telemetry_1.Lumberjack.debug(`New inbound web socket connection ${request.url}`);
            const socket = new socket_1.Socket(wsSocket);
            const subscriber = new RemoteSubscriber(socket);
            // Messages will be inbound from the remote server
            socket.on("message", (message) => {
                switch (message.type) {
                    case "connect": {
                        const connectMessage = message.payload;
                        const fullId = `${connectMessage.tenantId}/${connectMessage.documentId}`;
                        const orderer = this.orderMap.get(fullId);
                        (0, assert_1.default)(orderer);
                        // Create a new socket and bind it to a relay on the node
                        const connection = orderer.connectInternal(subscriber, (0, sillyname_1.default)().toLowerCase().split(" ").join("-"), connectMessage.client);
                        connection.connect().catch((err) => {
                            server_services_telemetry_1.Lumberjack.error("Error handling connect message", {
                                ...(0, server_services_telemetry_1.getLumberBaseProperties)(connectMessage.documentId, connectMessage.tenantId),
                            }, err);
                        });
                        // Need to subscribe to both channels. Then broadcast subscription across pipe
                        // on receiving a message
                        this.connectionMap.set(message.cid, connection);
                        // Emit connected message
                        const connected = {
                            clientId: connection.clientId,
                            existing: true,
                            maxMessageSize: this.maxMessageSize,
                            serviceConfiguration: server_services_core_1.DefaultServiceConfiguration,
                        };
                        socket.send({ cid: message.cid, type: "connected", payload: connected });
                        break;
                    }
                    case "disconnect": {
                        const connection = this.connectionMap.get(message.cid);
                        (0, assert_1.default)(connection);
                        connection.disconnect().catch((err) => {
                            server_services_telemetry_1.Lumberjack.error("Error handling disconnect message", undefined, err);
                        });
                        this.connectionMap.delete(message.cid);
                        break;
                    }
                    case "order": {
                        const orderMessage = message.payload;
                        const connection = this.connectionMap.get(message.cid);
                        (0, assert_1.default)(connection);
                        connection.order([orderMessage]).catch((err) => {
                            server_services_telemetry_1.Lumberjack.error("Error handling order message", undefined, err);
                        });
                        break;
                    }
                    default:
                        break;
                }
            });
        });
        this.webSocketServer.on("error", (error) => {
            (0, debug_1.debug)("wss error", error);
            server_services_telemetry_1.Lumberjack.error("wss error", undefined, error);
        });
    }
    async connectOrderer(tenantId, documentId) {
        const fullId = `${tenantId}/${documentId}`;
        // Our node is responsible for sequencing messages
        (0, debug_1.debug)(`${this.id} Becoming leader for ${fullId}`);
        server_services_telemetry_1.Lumberjack.debug(`${this.id} Becoming leader for ${fullId}`);
        const orderer = await localOrderer_1.LocalOrderer.load(this.storage, this.databaseManager, tenantId, documentId, this.logger, this.documentRepository, this.deliCheckpointRepository, this.scribeCheckpointRepository, this.deliCheckpointService, this.scribeCheckpointService);
        (0, assert_1.default)(!this.orderMap.has(fullId));
        this.orderMap.set(fullId, orderer);
        return orderer;
    }
    scheduleHeartbeat() {
        const now = Date.now();
        // Check to see if we can even renew at this point
        if (now > this.node.expiration) {
            // Have lost the node. Need to shutdown everything and close down
            (0, debug_1.debug)(`${this.node._id} did not renew before expiration`);
            server_services_telemetry_1.Lumberjack.debug(`${this.node._id} did not renew before expiration`);
            this.emit("expired");
            // TODO close the web socket server
        }
        else {
            // Schedule a heartbeat at the midpoint of the timeout length
            const targetTime = this.node.expiration - this.timeoutLength / 2;
            const delta = Math.max(0, targetTime - Date.now());
            setTimeout(() => {
                const updateP = LocalNode.updateExpiration(this.node, this.databaseManager, this.timeoutLength);
                updateP
                    .then((newNode) => {
                    // Debug(`Successfully renewed expiration for ${this.node._id}`);
                    this.node = newNode;
                    this.scheduleHeartbeat();
                })
                    .catch((error) => {
                    // Try again immediately.
                    (0, debug_1.debug)(`Failed to renew expiration for ${this.node._id}`, error);
                    server_services_telemetry_1.Lumberjack.error(`Failed to renew expiration for ${this.node._id}`, undefined, error);
                    this.scheduleHeartbeat();
                });
            }, delta);
        }
    }
}
exports.LocalNode = LocalNode;
//# sourceMappingURL=localNode.js.map