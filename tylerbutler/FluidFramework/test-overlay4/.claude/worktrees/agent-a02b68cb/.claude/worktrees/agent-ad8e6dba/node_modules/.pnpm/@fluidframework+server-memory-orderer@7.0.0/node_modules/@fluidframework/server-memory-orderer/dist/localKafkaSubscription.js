"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LocalKafkaSubscription = void 0;
const events_1 = require("events");
const server_services_telemetry_1 = require("@fluidframework/server-services-telemetry");
/**
 * A subscription for a single lambda
 *
 * @privateRemarks TODO: use context checkpoints
 */
class LocalKafkaSubscription extends events_1.EventEmitter {
    constructor(subscriber, queue) {
        super();
        this.subscriber = subscriber;
        this.queue = queue;
        this.queueOffset = 0;
        this.closed = false;
        this.processing = false;
    }
    close() {
        this.closed = true;
        if (this.retryTimer) {
            clearTimeout(this.retryTimer);
            this.retryTimer = undefined;
        }
        this.removeAllListeners();
    }
    async process() {
        if (this.queue.length <= this.queueOffset ||
            this.processing ||
            this.retryTimer !== undefined ||
            this.closed) {
            return;
        }
        const message = this.queue.get(this.queueOffset);
        if (message !== undefined) {
            try {
                this.processing = true;
                const optionalPromise = this.subscriber.process(message);
                if (optionalPromise !== undefined) {
                    await optionalPromise;
                }
                this.queueOffset++;
                this.emit("processed", this.queueOffset);
            }
            catch (ex) {
                // Lambda failed to process the message
                this.subscriber.context.error(ex, { restart: false });
                this.retryTimer = setTimeout(() => {
                    this.retryTimer = undefined;
                    this.process().catch((e) => this.handleProcessError(e));
                }, 500);
                return;
            }
            finally {
                this.processing = false;
            }
        }
        // Process the next one
        this.process().catch((e) => this.handleProcessError(e));
    }
    handleProcessError(error) {
        server_services_telemetry_1.Lumberjack.error("Error in LocalKafkaSubscription.process()", undefined, error);
    }
}
exports.LocalKafkaSubscription = LocalKafkaSubscription;
//# sourceMappingURL=localKafkaSubscription.js.map