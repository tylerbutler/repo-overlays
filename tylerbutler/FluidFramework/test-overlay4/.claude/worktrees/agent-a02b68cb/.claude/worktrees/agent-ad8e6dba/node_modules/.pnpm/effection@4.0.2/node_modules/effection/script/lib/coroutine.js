"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createCoroutine = createCoroutine;
exports.useCoroutine = useCoroutine;
const contexts_js_1 = require("./contexts.js");
const delimiter_js_1 = require("./delimiter.js");
const reducer_js_1 = require("./reducer.js");
const result_js_1 = require("./result.js");
function createCoroutine({ operation, scope }) {
    let reducer = scope.expect(reducer_js_1.ReducerContext);
    let iterator = undefined;
    let routine = {
        runLevel: 0,
        scope,
        data: {
            get iterator() {
                if (!iterator) {
                    iterator = operation()[Symbol.iterator]();
                }
                return iterator;
            },
            exit: (resolve) => resolve((0, result_js_1.Ok)()),
        },
        next(result) {
            routine.data.exit((exitResult) => {
                routine.data.exit = (didExit) => didExit((0, result_js_1.Ok)());
                reducer.reduce([
                    scope.expect(contexts_js_1.Priority),
                    routine,
                    exitResult.ok ? result : exitResult,
                    scope.expect(delimiter_js_1.DelimiterContext).validator,
                    "next",
                ]);
            });
        },
        return(result) {
            routine.data.exit((exitResult) => {
                routine.data.exit = (didExit) => didExit((0, result_js_1.Ok)());
                reducer.reduce([
                    scope.expect(contexts_js_1.Priority),
                    routine,
                    exitResult.ok ? result : exitResult,
                    scope.expect(delimiter_js_1.DelimiterContext).validator,
                    "return",
                ]);
            });
        },
    };
    return routine;
}
function* useCoroutine() {
    return (yield {
        description: "useCoroutine()",
        enter: (resolve, routine) => {
            resolve((0, result_js_1.Ok)(routine));
            return (uninstalled) => uninstalled((0, result_js_1.Ok)());
        },
    });
}
//# sourceMappingURL=coroutine.js.map