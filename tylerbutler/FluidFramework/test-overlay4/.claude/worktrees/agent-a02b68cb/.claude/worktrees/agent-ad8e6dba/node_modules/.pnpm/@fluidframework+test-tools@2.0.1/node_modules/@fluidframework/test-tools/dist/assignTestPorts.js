"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.writePortMapFile = exports.getPackageInfo = void 0;
const child_process_1 = require("child_process");
const fs_1 = __importDefault(require("fs"));
const os_1 = __importDefault(require("os"));
const path_1 = __importDefault(require("path"));
/**
 * Gets and parses a PackageInfo for packages in the workspace.
 */
function getPackageInfo(packageManager) {
    try {
        const command = packageManager;
        const args = packageManager === 'pnpm' ? ["recursive", "list", "--json", "--depth=-1"] : ["list", "--json", "--all"];
        const child = (0, child_process_1.spawnSync)(command, args, {
            encoding: "utf8",
            // shell:true is required for Windows without a resolved path to pnpm.
            shell: true,
        });
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const info = JSON.parse(child.stdout);
        if (!Array.isArray(info)) {
            // eslint-disable-next-line unicorn/prefer-type-error
            throw new Error(`stdin input was not package array. Spawn result: ${JSON.stringify(child)}`);
        }
        return info;
    }
    catch (error) {
        console.error(error);
        process.exit(-1);
    }
}
exports.getPackageInfo = getPackageInfo;
function writePortMapFile(initialPort, packageManager) {
    const info = getPackageInfo(packageManager);
    // Assign a unique port to each package
    const portMap = {};
    let port = initialPort;
    for (const pkg of info) {
        if (pkg.name === undefined) {
            console.error("missing name in package info");
            process.exit(-1);
        }
        portMap[pkg.name] = port++;
    }
    // Write the mappings to a temporary file as kv pairs
    const portMapPath = path_1.default.join(os_1.default.tmpdir(), "testportmap.json");
    fs_1.default.writeFileSync(portMapPath, JSON.stringify(portMap));
}
exports.writePortMapFile = writePortMapFile;
//# sourceMappingURL=assignTestPorts.js.map