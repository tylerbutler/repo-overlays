import { createContext } from "./context.js";
import { box } from "./box.js";
import { Ok, unbox } from "./result.js";
export class TaskGroup {
    constructor() {
        Object.defineProperty(this, "tasks", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: new Set()
        });
    }
    add(task) {
        this.tasks.add(task);
    }
    delete(task) {
        this.tasks.delete(task);
    }
    *halt() {
        let total = Ok();
        while (this.tasks.size > 0) {
            let tasks = [...this.tasks].reverse();
            this.tasks.clear();
            for (let task of tasks) {
                let result = yield* box(task.halt);
                if (!result.ok) {
                    total = result;
                }
            }
        }
        unbox(total);
    }
}
export const TaskGroupContext = createContext("@effection/task-group", new TaskGroup());
export function encapsulate(operation) {
    return TaskGroupContext.with(new TaskGroup(), function* (group) {
        try {
            return yield* operation();
        }
        finally {
            yield* group.halt();
        }
    });
}
//# sourceMappingURL=task-group.js.map