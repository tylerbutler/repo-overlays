"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.createNewFluidFileFromSummary = exports.renameEmptyFluidFile = exports.createNewEmptyFluidFile = exports.createNewFluidFile = void 0;
const internal_1 = require("@fluidframework/core-utils/internal");
const internal_2 = require("@fluidframework/driver-utils/internal");
const internal_3 = require("@fluidframework/odsp-driver-definitions/internal");
const internal_4 = require("@fluidframework/telemetry-utils/internal");
const contractsPublic_js_1 = require("./../contractsPublic.js");
const createOdspUrl_js_1 = require("./../createOdspUrl.js");
const getUrlAndHeadersWithAuth_js_1 = require("./../getUrlAndHeadersWithAuth.js");
const odspDriverUrlResolver_js_1 = require("./../odspDriverUrlResolver.js");
const odspUrlHelper_js_1 = require("./../odspUrlHelper.js");
const odspUtils_js_1 = require("./../odspUtils.js");
const packageVersion_js_1 = require("./../packageVersion.js");
const retryUtils_js_1 = require("./../retryUtils.js");
const createNewUtils_js_1 = require("./createNewUtils.js");
const isInvalidFileName = (fileName) => {
    const invalidCharsRegex = /["*/:<>?\\|]+/g;
    return invalidCharsRegex.test(fileName);
};
/**
 * Creates a new Fluid file.
 * Returns resolved url
 */
async function createNewFluidFile(getAuthHeader, newFileInfo, logger, createNewSummary, epochTracker, fileEntry, createNewCaching, forceAccessTokenViaAuthorizationHeader, isClpCompliantApp, enableSingleRequestForShareLinkWithCreate, resolvedUrl) {
    // Check for valid filename before the request to create file is actually made.
    if (isInvalidFileName(newFileInfo.filename)) {
        throw new internal_2.NonRetryableError(
        // pre-0.58 error message: Invalid filename
        "Invalid filename for createNew", internal_3.OdspErrorTypes.invalidFileNameError, { driverVersion: packageVersion_js_1.pkgVersion });
    }
    let itemId;
    let pendingRename;
    let summaryHandle = "";
    let shareLinkInfo;
    if (createNewSummary === undefined) {
        const content = await createNewEmptyFluidFile(getAuthHeader, newFileInfo, logger, epochTracker);
        itemId = content.itemId;
        pendingRename = newFileInfo.filename;
    }
    else {
        const content = await createNewFluidFileFromSummary(getAuthHeader, newFileInfo, logger, createNewSummary, epochTracker, forceAccessTokenViaAuthorizationHeader);
        itemId = content.itemId;
        summaryHandle = content.id;
        shareLinkInfo = extractShareLinkData(content, enableSingleRequestForShareLinkWithCreate);
    }
    const odspUrl = (0, createOdspUrl_js_1.createOdspUrl)({ ...newFileInfo, itemId, dataStorePath: "/" });
    const resolver = new odspDriverUrlResolver_js_1.OdspDriverUrlResolver();
    const odspResolvedUrl = await resolver.resolve({
        url: odspUrl,
        headers: { [contractsPublic_js_1.ClpCompliantAppHeader.isClpCompliantApp]: isClpCompliantApp },
    });
    fileEntry.docId = odspResolvedUrl.hashedDocumentId;
    fileEntry.resolvedUrl = odspResolvedUrl;
    odspResolvedUrl.context = resolvedUrl?.context;
    odspResolvedUrl.appName = resolvedUrl?.appName;
    odspResolvedUrl.codeHint = odspResolvedUrl.codeHint?.containerPackageName
        ? odspResolvedUrl.codeHint
        : resolvedUrl?.codeHint;
    if (shareLinkInfo?.createLink?.link) {
        let newWebUrl = shareLinkInfo.createLink.link.webUrl;
        newWebUrl = (0, odspUtils_js_1.appendNavParam)(newWebUrl, odspResolvedUrl, odspResolvedUrl.dataStorePath ?? "/", odspResolvedUrl.codeHint?.containerPackageName);
        shareLinkInfo.createLink.link.webUrl = newWebUrl;
    }
    odspResolvedUrl.shareLinkInfo = shareLinkInfo;
    odspResolvedUrl.pendingRename = pendingRename;
    if (createNewSummary !== undefined && createNewCaching) {
        (0, internal_1.assert)(summaryHandle !== undefined, 0x203 /* "Summary handle is undefined" */);
        // converting summary and getting sequence number
        const snapshot = (0, createNewUtils_js_1.convertCreateNewSummaryTreeToTreeAndBlobs)(createNewSummary, summaryHandle);
        // caching the converted summary
        await epochTracker.put((0, odspUtils_js_1.createCacheSnapshotKey)(odspResolvedUrl, (0, odspUtils_js_1.snapshotWithLoadingGroupIdSupported)((0, internal_4.loggerToMonitoringContext)(logger).config)), snapshot);
    }
    return odspResolvedUrl;
}
exports.createNewFluidFile = createNewFluidFile;
/**
 * If user requested creation of a sharing link along with the creation of the file by providing
 * createLinkScope in the request parameters then extract and save the sharing link information from
 * the response if it is available.
 * In case there was an error in creation of the sharing link, error is provided back in the response,
 * and does not impact the creation of file in ODSP.
 * @param requestedSharingLinkKind - Kind of sharing link requested to be created along with the creation of file.
 * @param response - Response object received from the /snapshot api call
 * @returns Sharing link information received in the response from a successful creation of a file.
 */
function extractShareLinkData(response, enableSingleRequestForShareLinkWithCreate) {
    let shareLinkInfo;
    if (enableSingleRequestForShareLinkWithCreate) {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
        const { sharing } = response;
        if (!sharing) {
            return;
        }
        /* eslint-disable @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-member-access */
        shareLinkInfo = {
            createLink: {
                link: sharing.sharingLink
                    ? {
                        scope: sharing.sharingLink.scope,
                        role: sharing.sharingLink.type,
                        webUrl: sharing.sharingLink.webUrl,
                        ...sharing.sharingLink,
                    }
                    : undefined,
                error: sharing.error,
                shareId: sharing.shareId,
            },
        };
        /* eslint-enable @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-member-access */
    }
    return shareLinkInfo;
}
/**
 * Encodes file path so it can be embedded in the request url
 * @param path - path to encode
 * @returns encoded path or "" if path is undefined
 */
function encodeFilePath(path) {
    return path ? encodeURIComponent(path.startsWith("/") ? path : `/${path}`) : "";
}
async function createNewEmptyFluidFile(getAuthHeader, newFileInfo, logger, epochTracker) {
    const filePath = encodeFilePath(newFileInfo.filePath);
    const encodedFilename = encodeURIComponent(`${newFileInfo.filename}.tmp`);
    const initialUrl = `${(0, odspUrlHelper_js_1.getApiRoot)(new URL(newFileInfo.siteUrl))}/drives/${newFileInfo.driveId}/items/root:${filePath}/${encodedFilename}:/content?@name.conflictBehavior=rename&select=id,name,parentReference`;
    return (0, odspUtils_js_1.getWithRetryForTokenRefresh)(async (options) => {
        const url = initialUrl;
        const method = "PUT";
        const authHeader = await getAuthHeader({ ...options, request: { url, method } }, "CreateNewFile");
        const internalFarmType = (0, odspUrlHelper_js_1.checkForKnownServerFarmType)(newFileInfo.siteUrl);
        return internal_4.PerformanceEvent.timedExecAsync(logger, {
            eventName: "createNewEmptyFile",
            details: {
                internalFarmType,
            },
        }, async (event) => {
            const headers = (0, getUrlAndHeadersWithAuth_js_1.getHeadersWithAuth)(authHeader);
            headers["Content-Type"] = "application/json";
            const fetchResponse = await (0, retryUtils_js_1.runWithRetry)(async () => epochTracker.fetchAndParseAsJSON(url, {
                body: undefined,
                headers,
                method,
            }, "createFile"), "createFile", logger);
            const content = fetchResponse.content;
            if (!content?.id) {
                throw new internal_2.NonRetryableError(
                // pre-0.58 error message: ODSP CreateFile call returned no item ID
                "ODSP CreateFile call returned no item ID (for empty file)", internal_3.OdspErrorTypes.incorrectServerResponse, { driverVersion: packageVersion_js_1.pkgVersion });
            }
            event.end({
                ...fetchResponse.propsToLog,
            });
            return { itemId: content.id, fileName: content.name };
        }, { end: true, cancel: "error" });
    });
}
exports.createNewEmptyFluidFile = createNewEmptyFluidFile;
async function renameEmptyFluidFile(getAuthHeader, odspParts, requestedFileName, logger, epochTracker) {
    const initialUrl = `${(0, odspUrlHelper_js_1.getApiRoot)(new URL(odspParts.siteUrl))}/drives/${odspParts.driveId}/items/${odspParts.itemId}?@name.conflictBehavior=rename`;
    return (0, odspUtils_js_1.getWithRetryForTokenRefresh)(async (options) => {
        const url = initialUrl;
        const method = "PATCH";
        const authHeader = await getAuthHeader({ ...options, request: { url, method } }, "renameFile");
        return internal_4.PerformanceEvent.timedExecAsync(logger, { eventName: "renameFile" }, async (event) => {
            const headers = (0, getUrlAndHeadersWithAuth_js_1.getHeadersWithAuth)(authHeader);
            headers["Content-Type"] = "application/json";
            const fetchResponse = await (0, retryUtils_js_1.runWithRetry)(async () => epochTracker.fetchAndParseAsJSON(url, {
                body: JSON.stringify({
                    name: requestedFileName,
                }),
                headers,
                method: "PATCH",
            }, "renameFile"), "renameFile", logger);
            const content = fetchResponse.content;
            if (!content?.id) {
                throw new internal_2.NonRetryableError("ODSP RenameFile call returned no item ID (for empty file)", internal_3.OdspErrorTypes.incorrectServerResponse, { driverVersion: packageVersion_js_1.pkgVersion });
            }
            event.end({
                ...fetchResponse.propsToLog,
            });
            return content;
        }, { end: true, cancel: "error" });
    });
}
exports.renameEmptyFluidFile = renameEmptyFluidFile;
async function createNewFluidFileFromSummary(getAuthHeader, newFileInfo, logger, createNewSummary, epochTracker, forceAccessTokenViaAuthorizationHeader) {
    const filePath = encodeFilePath(newFileInfo.filePath);
    const encodedFilename = encodeURIComponent(newFileInfo.filename);
    const baseUrl = `${(0, odspUrlHelper_js_1.getApiRoot)(new URL(newFileInfo.siteUrl))}/drives/${newFileInfo.driveId}/items/root:` +
        `${filePath}/${encodedFilename}`;
    const containerSnapshot = (0, createNewUtils_js_1.convertSummaryIntoContainerSnapshot)(createNewSummary);
    // Build share link parameter based on the createLinkType provided so that the
    // snapshot api can create and return the share link along with creation of file in the response.
    const createShareLinkParam = (0, odspUtils_js_1.buildOdspShareLinkReqParams)(newFileInfo.createLinkType);
    const initialUrl = `${baseUrl}:/opStream/snapshots/snapshot${createShareLinkParam ? `?${createShareLinkParam}` : ""}`;
    return (0, createNewUtils_js_1.createNewFluidContainerCore)({
        containerSnapshot,
        getAuthHeader,
        logger,
        initialUrl,
        forceAccessTokenViaAuthorizationHeader,
        epochTracker,
        telemetryName: "CreateNewFile",
        fetchType: "createFile",
        validateResponseCallback: (content) => {
            if (!content?.itemId) {
                throw new internal_2.NonRetryableError("ODSP CreateFile call returned no item ID", internal_3.OdspErrorTypes.incorrectServerResponse, { driverVersion: packageVersion_js_1.pkgVersion });
            }
        },
    });
}
exports.createNewFluidFileFromSummary = createNewFluidFileFromSummary;
//# sourceMappingURL=createFile.js.map