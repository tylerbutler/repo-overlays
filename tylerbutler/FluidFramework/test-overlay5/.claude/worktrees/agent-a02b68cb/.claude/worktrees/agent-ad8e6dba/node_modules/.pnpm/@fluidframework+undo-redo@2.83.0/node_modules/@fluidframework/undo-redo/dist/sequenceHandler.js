"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SharedSegmentSequenceRevertible = exports.SharedSegmentSequenceUndoRedoHandler = void 0;
const internal_1 = require("@fluidframework/merge-tree/internal");
/**
 * A shared segment sequence undo redo handler that will add all local sequences changes to the provided
 * undo redo stack manager
 * @internal
 */
class SharedSegmentSequenceUndoRedoHandler {
    constructor(stackManager) {
        this.stackManager = stackManager;
        this.sequences = new Map();
        this.sequenceDeltaHandler = (event, target) => {
            if (event.isLocal) {
                let revertible = this.sequences.get(target);
                if (revertible === undefined) {
                    revertible = new SharedSegmentSequenceRevertible(target);
                    this.stackManager.pushToCurrentOperation(revertible);
                    this.sequences.set(target, revertible);
                }
                revertible.add(event);
            }
        };
        this.stackManager.on("changePushed", () => this.sequences.clear());
    }
    attachSequence(sequence) {
        sequence.on("sequenceDelta", this.sequenceDeltaHandler);
    }
    detachSequence(sequence) {
        sequence.off("sequenceDelta", this.sequenceDeltaHandler);
    }
}
exports.SharedSegmentSequenceUndoRedoHandler = SharedSegmentSequenceUndoRedoHandler;
/**
 * Tracks a change on a shared segment sequence and allows reverting it
 * @internal
 */
class SharedSegmentSequenceRevertible {
    constructor(sequence) {
        this.sequence = sequence;
        this.revertibles = [];
    }
    add(event) {
        (0, internal_1.appendToMergeTreeDeltaRevertibles)(event.deltaArgs, this.revertibles);
    }
    revert() {
        (0, internal_1.revertMergeTreeDeltaRevertibles)(this.sequence, this.revertibles);
    }
    discard() {
        (0, internal_1.discardMergeTreeDeltaRevertible)(this.revertibles);
    }
}
exports.SharedSegmentSequenceRevertible = SharedSegmentSequenceRevertible;
//# sourceMappingURL=sequenceHandler.js.map