/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
import { type ISegment } from "@fluidframework/merge-tree/internal";
import type { SequenceDeltaEvent, ISharedSegmentSequence } from "@fluidframework/sequence/internal";
import type { IRevertible, UndoRedoStackManager } from "./undoRedoStackManager.js";
/**
 * A shared segment sequence undo redo handler that will add all local sequences changes to the provided
 * undo redo stack manager
 * @internal
 */
export declare class SharedSegmentSequenceUndoRedoHandler {
    private readonly stackManager;
    private readonly sequences;
    constructor(stackManager: UndoRedoStackManager);
    attachSequence<T extends ISegment>(sequence: ISharedSegmentSequence<T>): void;
    detachSequence<T extends ISegment>(sequence: ISharedSegmentSequence<T>): void;
    private readonly sequenceDeltaHandler;
}
/**
 * Tracks a change on a shared segment sequence and allows reverting it
 * @internal
 */
export declare class SharedSegmentSequenceRevertible implements IRevertible {
    readonly sequence: ISharedSegmentSequence<ISegment>;
    private readonly revertibles;
    constructor(sequence: ISharedSegmentSequence<ISegment>);
    add(event: SequenceDeltaEvent): void;
    revert(): void;
    discard(): void;
}
//# sourceMappingURL=sequenceHandler.d.ts.map