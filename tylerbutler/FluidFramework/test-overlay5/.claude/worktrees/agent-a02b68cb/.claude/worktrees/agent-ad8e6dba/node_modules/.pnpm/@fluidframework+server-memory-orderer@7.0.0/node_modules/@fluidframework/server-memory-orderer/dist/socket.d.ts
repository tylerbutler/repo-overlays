/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
/// <reference types="node" />
import { EventEmitter } from "events";
import { IsoBuffer } from "@fluidframework/common-utils";
import ws from "ws";
export declare class Socket<T> extends EventEmitter {
    private readonly socket;
    static connect<T>(address: string, path: string): Promise<Socket<T>>;
    private pending;
    private sendScheduled;
    constructor(socket: ws);
    /**
     * Sends the given value on the socket
     */
    send(value: T): void;
    on(event: "close", listener: (code: number, reason: string) => void): this;
    on(event: "error", listener: (err: Error) => void): this;
    on(event: "message", listener: (data: T) => void): this;
    on(event: "open", listener: () => void): this;
    on(event: "ping" | "pong", listener: (data: IsoBuffer) => void): this;
    private sendBuffers;
}
//# sourceMappingURL=socket.d.ts.map