/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
import { IClient } from "@fluidframework/protocol-definitions";
import { IOrderer, IOrdererConnection, IWebSocket } from "@fluidframework/server-services-core";
export interface IOrdererConnectionFactory {
    connect(socket: IWebSocket, client: IClient): Promise<IOrdererConnection>;
}
/**
 * Proxies ordering to an external service which does the actual ordering
 */
export declare class ProxyOrderer implements IOrderer {
    private readonly factory;
    constructor(factory: IOrdererConnectionFactory);
    connect(socket: IWebSocket, clientId: string, client: IClient): Promise<IOrdererConnection>;
    close(): Promise<void>;
}
//# sourceMappingURL=proxyOrderer.d.ts.map