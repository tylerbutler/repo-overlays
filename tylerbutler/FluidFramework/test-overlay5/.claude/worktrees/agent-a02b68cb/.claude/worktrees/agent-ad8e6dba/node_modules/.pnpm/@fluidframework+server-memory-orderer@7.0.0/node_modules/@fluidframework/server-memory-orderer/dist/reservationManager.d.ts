/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
/// <reference types="node" />
import { EventEmitter } from "events";
import { MongoManager } from "@fluidframework/server-services-core";
import { IConcreteNode, IReservationManager } from "./interfaces";
import { NodeManager } from "./nodeManager";
/**
 * Reservation for the given id within the system. The reservation is considered held for as long as the node
 * maintains the given epoch
 * @internal
 */
export interface IReservation {
    _id: string;
    node: string;
}
/**
 * @internal
 */
export declare class ReservationManager extends EventEmitter implements IReservationManager {
    private readonly nodeTracker;
    private readonly mongoManager;
    private readonly reservationColletionName;
    constructor(nodeTracker: NodeManager, mongoManager: MongoManager, reservationColletionName: string);
    getOrReserve(key: string, node: IConcreteNode): Promise<IConcreteNode>;
    private makeReservation;
    private getReservationsCollection;
}
//# sourceMappingURL=reservationManager.d.ts.map