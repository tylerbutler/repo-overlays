import { updatePackageJsonFile } from "@tylerbu/cli-api";
import { call } from "effection";
import { resolve as resolvePath } from "pathe";
import { sortPackageJson } from "sort-package-json";
import { definePackagePolicy } from "../policyDefiners/definePackagePolicy.js";
/**
 * A repo policy that checks if package.json files in the repo are sorted using sort-package-json.
 *
 * @alpha
 */
export const PackageJsonSorted = definePackagePolicy({
    name: "PackageJsonSorted",
    description: "Ensures package.json files are sorted consistently using sort-package-json.",
    handler: function* (json, { file, root, resolve }) {
        const sortedJson = sortPackageJson(json);
        const isSorted = JSON.stringify(sortedJson) === JSON.stringify(json);
        if (isSorted) {
            return true;
        }
        if (resolve) {
            try {
                yield* call(() => updatePackageJsonFile(resolvePath(root, file), (pkgJson) => pkgJson, {
                    sort: true,
                }));
                const result = {
                    name: PackageJsonSorted.name,
                    file,
                    resolved: true,
                    errorMessages: [],
                };
                return result;
            }
            catch (error) {
                const result = {
                    name: PackageJsonSorted.name,
                    file,
                    resolved: false,
                    autoFixable: true,
                    errorMessages: [
                        error.message,
                        error.stack ?? "",
                    ],
                };
                return result;
            }
        }
        else {
            const result = {
                name: PackageJsonSorted.name,
                file,
                autoFixable: true,
                errorMessages: [],
            };
            return result;
        }
    },
});
//# sourceMappingURL=PackageJsonSorted.js.map