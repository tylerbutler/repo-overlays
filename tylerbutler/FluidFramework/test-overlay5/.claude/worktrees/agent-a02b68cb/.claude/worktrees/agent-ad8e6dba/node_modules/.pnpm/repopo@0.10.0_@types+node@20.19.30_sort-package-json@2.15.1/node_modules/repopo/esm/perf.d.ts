import type { Logger } from "@tylerbu/cli-api";
import type { Operation } from "effection";
import type { PolicyName } from "./policy.js";
export type PolicyAction = "check" | "resolve" | "handle";
/**
 * Stores performance data for each handler. Used to collect and display performance stats.
 */
export interface PolicyHandlerPerfStats {
    count: number;
    processed: number;
    data: Map<PolicyAction, Map<PolicyName, number>>;
}
export declare function newPerfStats(): PolicyHandlerPerfStats;
export declare function runWithPerf<T>(name: string, action: PolicyAction, stats: PolicyHandlerPerfStats, run: () => Operation<T>): Operation<T>;
export declare function logStats(stats: PolicyHandlerPerfStats, log: Logger): void;
//# sourceMappingURL=perf.d.ts.map