"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.LocalKafka = void 0;
const server_services_telemetry_1 = require("@fluidframework/server-services-telemetry");
const double_ended_queue_1 = __importDefault(require("double-ended-queue"));
const localKafkaSubscription_1 = require("./localKafkaSubscription");
/**
 * Simple Kafka simulation
 * Lambdas can subscribe to messages.
 * Each subscription keeps track of its offset in the queue.
 * Queue is cleaned up once all subscriptions processed past the min.
 * @internal
 */
class LocalKafka {
    constructor(messageOffset = 0) {
        this.messageOffset = messageOffset;
        this.subscriptions = [];
        this.qeueue = new double_ended_queue_1.default();
        this.minimumQueueOffset = 0;
    }
    get length() {
        return this.qeueue.length;
    }
    isConnected() {
        return true;
    }
    subscribe(kafakaSubscriber) {
        const kafkaSubscription = new localKafkaSubscription_1.LocalKafkaSubscription(kafakaSubscriber, this.qeueue);
        kafkaSubscription.on("processed", (queueOffset) => {
            if (this.minimumQueueOffset >= queueOffset) {
                return;
            }
            // Check if this queueOffset is the min
            for (const subscription of this.subscriptions) {
                if (subscription.queueOffset < queueOffset) {
                    return;
                }
            }
            const diff = queueOffset - this.minimumQueueOffset;
            this.minimumQueueOffset = queueOffset - 1;
            // Remove items before min queue offset
            for (let i = 0; i < diff; i++) {
                this.qeueue.shift();
            }
            // Update offsets in each subscription to account for the queue index changing
            for (const subscription of this.subscriptions) {
                subscription.queueOffset -= diff;
            }
        });
        this.subscriptions.push(kafkaSubscription);
    }
    async send(messages, topic) {
        for (const message of messages) {
            const queuedMessage = {
                offset: this.messageOffset,
                partition: 0,
                topic,
                value: JSON.stringify(message),
            };
            this.messageOffset++;
            this.qeueue.push(queuedMessage);
        }
        for (const subscription of this.subscriptions) {
            subscription.process().catch((error) => {
                server_services_telemetry_1.Lumberjack.error("Error processing local kafka subscription", undefined, error);
            });
        }
    }
    async close() {
        this.qeueue.clear();
        for (const subscription of this.subscriptions) {
            subscription.close();
        }
        this.subscriptions.length = 0;
    }
    on(event, listener) {
        return this;
    }
    once(event, listener) {
        return this;
    }
    off(event, listener) {
        return this;
    }
}
exports.LocalKafka = LocalKafka;
//# sourceMappingURL=localKafka.js.map