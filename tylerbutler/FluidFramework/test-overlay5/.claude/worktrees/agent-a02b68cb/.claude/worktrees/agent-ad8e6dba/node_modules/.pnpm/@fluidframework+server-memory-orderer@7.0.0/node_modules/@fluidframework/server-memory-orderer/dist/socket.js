"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Socket = void 0;
const events_1 = require("events");
const ws_1 = __importDefault(require("ws"));
const debug_1 = require("./debug");
class Socket extends events_1.EventEmitter {
    static async connect(address, path) {
        const socket = new ws_1.default(`ws://${address}/${path}`);
        await new Promise((resolve, reject) => {
            const errorListener = (error) => reject(error);
            socket.on("error", errorListener);
            socket.on("open", () => {
                socket.removeListener("error", errorListener);
                resolve();
            });
        });
        return new Socket(socket);
    }
    constructor(socket) {
        super();
        this.socket = socket;
        this.pending = [];
        this.sendScheduled = false;
        this.socket.on("error", (error) => {
            (0, debug_1.debug)(`ws error on connection to ${socket.url}`, error);
            this.emit("error", error);
        });
        this.socket.on("close", (code, reason) => {
            (0, debug_1.debug)(`ws to ${socket.url} close ${code} ${reason}`);
            this.emit("close", code, reason);
        });
        this.socket.on("message", (messagesJson) => {
            const parsedMessages = JSON.parse(messagesJson);
            for (const parsedMessage of parsedMessages) {
                this.emit("message", parsedMessage);
            }
        });
    }
    /**
     * Sends the given value on the socket
     */
    send(value) {
        this.pending.push(value);
        if (this.sendScheduled) {
            return;
        }
        this.sendScheduled = true;
        setImmediate(() => {
            (0, debug_1.debug)(`Sending ${this.pending.length} messages`);
            this.sendBuffers(this.pending);
            this.pending = [];
            this.sendScheduled = false;
        });
    }
    on(event, listener) {
        super.on(event, listener);
        return this;
    }
    sendBuffers(buffers) {
        this.socket.send(JSON.stringify(buffers));
    }
}
exports.Socket = Socket;
//# sourceMappingURL=socket.js.map