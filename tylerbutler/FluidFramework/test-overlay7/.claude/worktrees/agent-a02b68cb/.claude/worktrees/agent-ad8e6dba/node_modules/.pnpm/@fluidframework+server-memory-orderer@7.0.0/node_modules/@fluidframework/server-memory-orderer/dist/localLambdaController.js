"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LocalLambdaController = void 0;
const events_1 = require("events");
const server_services_core_1 = require("@fluidframework/server-services-core");
const server_services_telemetry_1 = require("@fluidframework/server-services-telemetry");
/**
 * Controls lambda startups and subscriptions for localOrderer
 * @internal
 */
class LocalLambdaController extends events_1.EventEmitter {
    constructor(kafaka, setup, context, starter) {
        super();
        this.kafaka = kafaka;
        this.setup = setup;
        this.context = context;
        this.starter = starter;
        this._state = "created";
        this.kafaka.subscribe(this);
    }
    get state() {
        return this._state;
    }
    async start() {
        if (this._state === "closed") {
            return;
        }
        try {
            this.lambda = await this.starter(this.setup, this.context);
            if (this._state === "created") {
                this._state = "started";
            }
            this.emit("started", this.lambda);
            // eslint-disable-next-line @typescript-eslint/ban-ts-comment
            // @ts-ignore
            if (this._state === "closed") {
                // Close was probably called while starting
                this.close();
            }
        }
        catch (ex) {
            // In the event a lambda fails to start, retry it
            this.context.error(ex, { restart: true });
            this.startTimer = setTimeout(() => {
                this.start().catch((err) => {
                    server_services_telemetry_1.Lumberjack.error("Error starting local lambda controller", undefined, err);
                });
            }, 5000);
        }
    }
    close() {
        this._state = "closed";
        if (this.lambda) {
            this.lambda.close(server_services_core_1.LambdaCloseType.Stop);
            this.lambda = undefined;
        }
        if (this.startTimer !== undefined) {
            clearTimeout(this.startTimer);
            this.startTimer = undefined;
        }
        this.removeAllListeners();
    }
    // eslint-disable-next-line @typescript-eslint/promise-function-async
    process(message) {
        if (!this.lambda) {
            throw new Error("The lambda has not started yet");
        }
        return this.lambda.handler(message);
    }
}
exports.LocalLambdaController = LocalLambdaController;
//# sourceMappingURL=localLambdaController.js.map