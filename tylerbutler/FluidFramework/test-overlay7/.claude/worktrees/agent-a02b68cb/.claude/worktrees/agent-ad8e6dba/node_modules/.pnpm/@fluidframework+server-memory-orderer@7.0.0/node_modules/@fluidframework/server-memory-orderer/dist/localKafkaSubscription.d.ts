/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
/// <reference types="node" />
import { EventEmitter } from "events";
import { IQueuedMessage } from "@fluidframework/server-services-core";
import * as Deque from "double-ended-queue";
import { IKafkaSubscriber } from "./interfaces";
/**
 * A subscription for a single lambda
 *
 * @privateRemarks TODO: use context checkpoints
 */
export declare class LocalKafkaSubscription extends EventEmitter {
    private readonly subscriber;
    private readonly queue;
    queueOffset: number;
    private closed;
    private processing;
    private retryTimer;
    constructor(subscriber: IKafkaSubscriber, queue: Deque<IQueuedMessage>);
    close(): void;
    process(): Promise<void>;
    private handleProcessError;
}
//# sourceMappingURL=localKafkaSubscription.d.ts.map