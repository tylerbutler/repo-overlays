"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LocalOrdererSetup = void 0;
const common_utils_1 = require("@fluidframework/common-utils");
const server_services_telemetry_1 = require("@fluidframework/server-services-telemetry");
class LocalOrdererSetup {
    constructor(tenantId, documentId, storage, databaseManager, documentRepository, deliCheckpointRepository, scribeCheckpointRepository, deliCheckpointService, scribeCheckpointService, gitManager) {
        this.tenantId = tenantId;
        this.documentId = documentId;
        this.storage = storage;
        this.databaseManager = databaseManager;
        this.documentRepository = documentRepository;
        this.deliCheckpointRepository = deliCheckpointRepository;
        this.scribeCheckpointRepository = scribeCheckpointRepository;
        this.deliCheckpointService = deliCheckpointService;
        this.scribeCheckpointService = scribeCheckpointService;
        this.gitManager = gitManager;
    }
    // eslint-disable-next-line @typescript-eslint/promise-function-async
    documentP() {
        return this.storage.getOrCreateDocument(this.tenantId, this.documentId);
    }
    /**
     * @deprecated use documentRepositoryP() instead
     */
    // eslint-disable-next-line @typescript-eslint/promise-function-async
    documentCollectionP() {
        server_services_telemetry_1.Lumberjack.error("documentCollectionP() is deprecated but still used.");
        return this.databaseManager.getDocumentCollection();
    }
    // eslint-disable-next-line @typescript-eslint/promise-function-async
    localCheckpointCollectionP() {
        return this.databaseManager.getCheckpointCollection();
    }
    async documentRepositoryP() {
        return this.documentRepository;
    }
    async deliCheckpointRepositoryP() {
        return this.deliCheckpointRepository;
    }
    async scribeCheckpointRepositoryP() {
        return this.scribeCheckpointRepository;
    }
    async checkpointServiceP(service) {
        return service === "deli" ? this.deliCheckpointService : this.scribeCheckpointService;
    }
    // eslint-disable-next-line @typescript-eslint/promise-function-async
    deltaCollectionP() {
        return this.databaseManager.getDeltaCollection(this.tenantId, this.documentId);
    }
    // eslint-disable-next-line @typescript-eslint/promise-function-async
    scribeDeltaCollectionP() {
        return this.databaseManager.getScribeDeltaCollection(this.tenantId, this.documentId);
    }
    async protocolHeadP() {
        if (!this.gitManager) {
            return 0;
        }
        const existingRef = await this.gitManager.getRef(encodeURIComponent(this.documentId));
        if (!existingRef) {
            return -1;
        }
        const content = await this.gitManager.getContent(existingRef.object.sha, ".protocol/attributes");
        const attributes = JSON.parse((0, common_utils_1.fromBase64ToUtf8)(content.content));
        return attributes.sequenceNumber;
    }
    async scribeMessagesP() {
        const scribeDeltaCollection = await this.scribeDeltaCollectionP();
        return scribeDeltaCollection.find({
            documentId: this.documentId,
            tenantId: this.tenantId,
        }, { "operation.sequenceNumber": 1 });
    }
}
exports.LocalOrdererSetup = LocalOrdererSetup;
//# sourceMappingURL=localOrdererSetup.js.map