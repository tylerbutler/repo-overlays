"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReservationManager = void 0;
const events_1 = require("events");
/**
 * @internal
 */
class ReservationManager extends events_1.EventEmitter {
    constructor(nodeTracker, mongoManager, reservationColletionName) {
        super();
        this.nodeTracker = nodeTracker;
        this.mongoManager = mongoManager;
        this.reservationColletionName = reservationColletionName;
    }
    async getOrReserve(key, node) {
        const reservations = await this.getReservationsCollection();
        const reservation = await reservations.findOne({ _id: key });
        // Reservation can be null (first time), expired, or existing and within the time window
        if (reservation === null) {
            await this.makeReservation(node, key, undefined, reservations);
            return node;
        }
        else {
            const remoteNode = await this.nodeTracker.loadRemote(reservation.node);
            if (remoteNode.valid) {
                return remoteNode;
            }
            else {
                await this.makeReservation(node, key, reservation, reservations);
                return node;
            }
        }
    }
    async makeReservation(node, key, existing, collection) {
        const newReservation = { _id: key, node: node.id };
        await (existing !== undefined
            ? collection.update({ _id: key, node: existing.node }, newReservation, null)
            : collection.insertOne(newReservation));
    }
    async getReservationsCollection() {
        const db = await this.mongoManager.getDatabase();
        return db.collection(this.reservationColletionName);
    }
}
exports.ReservationManager = ReservationManager;
//# sourceMappingURL=reservationManager.js.map