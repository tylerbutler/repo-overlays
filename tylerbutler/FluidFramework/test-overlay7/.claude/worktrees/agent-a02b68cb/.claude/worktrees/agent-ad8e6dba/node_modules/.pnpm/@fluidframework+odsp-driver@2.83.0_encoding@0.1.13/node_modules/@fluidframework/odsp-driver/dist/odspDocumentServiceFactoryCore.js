"use strict";
/*!
 * Copyright (c) Microsoft Corporation and contributors. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.OdspDocumentServiceFactoryCore = void 0;
const internal_1 = require("@fluidframework/driver-utils/internal");
const internal_2 = require("@fluidframework/odsp-driver-definitions/internal");
const internal_3 = require("@fluidframework/telemetry-utils/internal");
const uuid_1 = require("uuid");
const index_js_1 = require("./createFile/index.js");
const epochTracker_js_1 = require("./epochTracker.js");
const odspCache_js_1 = require("./odspCache.js");
const odspDocumentService_js_1 = require("./odspDocumentService.js");
const odspLayerCompatState_js_1 = require("./odspLayerCompatState.js");
const odspUtils_js_1 = require("./odspUtils.js");
/**
 * Factory for creating the sharepoint document service. Use this if you want to
 * use the sharepoint implementation.
 *
 * This constructor should be used by environments that support dynamic imports and that wish
 * to leverage code splitting as a means to keep bundles as small as possible.
 * @legacy
 * @beta
 */
class OdspDocumentServiceFactoryCore {
    get snapshotPrefetchResultCache() {
        return this.nonPersistentCache.snapshotPrefetchResultCache;
    }
    // TODO: return `IRelaySessionAwareDriverFactory` instead of `this` (breaking change)
    get IRelaySessionAwareDriverFactory() {
        return this;
    }
    /**
     * This function would return info about relay service session only if this factory established (or attempted to
     * establish) connection very recently. Otherwise, it will return undefined.
     * @param resolvedUrl - resolved url for container
     * @returns The current join session response stored in cache. `undefined` if not present.
     */
    async getRelayServiceSessionInfo(resolvedUrl) {
        const odspResolvedUrl = (0, odspUtils_js_1.getOdspResolvedUrl)(resolvedUrl);
        const joinSessionResponse = await this.nonPersistentCache.sessionJoinCache.get((0, odspUtils_js_1.getJoinSessionCacheKey)(odspResolvedUrl));
        return joinSessionResponse?.joinSessionResponse;
    }
    async createContainer(createNewSummary, createNewResolvedUrl, logger, clientIsSummarizer) {
        const odspResolvedUrl = (0, odspUtils_js_1.getOdspResolvedUrl)(createNewResolvedUrl);
        const resolvedUrlData = {
            siteUrl: odspResolvedUrl.siteUrl,
            driveId: odspResolvedUrl.driveId,
            itemId: odspResolvedUrl.itemId,
        };
        let fileInfo;
        let createShareLinkParam;
        if (odspResolvedUrl.itemId) {
            fileInfo = {
                type: "Existing",
                driveId: odspResolvedUrl.driveId,
                siteUrl: odspResolvedUrl.siteUrl,
                itemId: odspResolvedUrl.itemId,
            };
        }
        else if (odspResolvedUrl.fileName) {
            const [, queryString] = odspResolvedUrl.url.split("?");
            const searchParams = new URLSearchParams(queryString);
            const filePath = searchParams.get("path");
            if (filePath === undefined || filePath === null) {
                throw new Error("File path should be provided!!");
            }
            createShareLinkParam = getSharingLinkParams(this.hostPolicy, searchParams);
            fileInfo = {
                type: "New",
                driveId: odspResolvedUrl.driveId,
                siteUrl: odspResolvedUrl.siteUrl,
                filePath,
                filename: odspResolvedUrl.fileName,
                createLinkType: createShareLinkParam,
            };
        }
        else {
            throw new Error("A new or existing file must be specified to create container!");
        }
        if ((0, internal_1.isCombinedAppAndProtocolSummary)(createNewSummary)) {
            const documentAttributes = (0, internal_1.getDocAttributesFromProtocolSummary)(createNewSummary.tree[".protocol"]);
            if (documentAttributes?.sequenceNumber !== 0) {
                throw new Error("Seq number in detached ODSP container should be 0");
            }
        }
        const odspLogger = (0, odspUtils_js_1.createOdspLogger)(logger);
        const fileEntry = {
            resolvedUrl: odspResolvedUrl,
            docId: odspResolvedUrl.hashedDocumentId,
            fileVersion: undefined,
        };
        const cacheAndTracker = (0, epochTracker_js_1.createOdspCacheAndTracker)(this.persistedCache, this.nonPersistentCache, fileEntry, odspLogger, clientIsSummarizer);
        return internal_3.PerformanceEvent.timedExecAsync(odspLogger, {
            eventName: "CreateNew",
            isWithSummaryUpload: true,
            createShareLinkParam: createShareLinkParam
                ? JSON.stringify(createShareLinkParam)
                : undefined,
            enableSingleRequestForShareLinkWithCreate: this.hostPolicy.enableSingleRequestForShareLinkWithCreate,
        }, async (event) => {
            const getAuthHeader = (0, odspUtils_js_1.toInstrumentedOdspStorageTokenFetcher)(odspLogger, resolvedUrlData, this.getStorageToken);
            const _odspResolvedUrl = await (0, index_js_1.useCreateNewModule)(odspLogger, async (module) => {
                return (0, odspUtils_js_1.isNewFileInfo)(fileInfo)
                    ? module.createNewFluidFile(getAuthHeader, fileInfo, odspLogger, createNewSummary, cacheAndTracker.epochTracker, fileEntry, this.hostPolicy.cacheCreateNewSummary ?? true, !!this.hostPolicy.sessionOptions?.forceAccessTokenViaAuthorizationHeader, odspResolvedUrl.isClpCompliantApp, this.hostPolicy.enableSingleRequestForShareLinkWithCreate, odspResolvedUrl)
                    : module.createNewContainerOnExistingFile(getAuthHeader, fileInfo, odspLogger, createNewSummary, cacheAndTracker.epochTracker, fileEntry, this.hostPolicy.cacheCreateNewSummary ?? true, !!this.hostPolicy.sessionOptions?.forceAccessTokenViaAuthorizationHeader, odspResolvedUrl.isClpCompliantApp, odspResolvedUrl.fileMetadata?.eTag);
            });
            const docService = this.createDocumentServiceCore(_odspResolvedUrl, odspLogger, cacheAndTracker, clientIsSummarizer);
            event.end({
                docId: _odspResolvedUrl.hashedDocumentId,
            });
            return docService;
        });
    }
    /**
     * @param getStorageToken - function that can provide the storage token for a given site. This is
     * is also referred to as the "Vroom" token in SPO.
     * @param getWebsocketToken - function that can provide a token for accessing the web socket. This is also
     * to as the "Push" token in SPO. If undefined then websocket token is expected to be returned with joinSession
     * response payload.
     * @param persistedCache - PersistedCache provided by host for use in this session.
     * @param hostPolicy - Policy for storage provided by host.
     */
    constructor(getStorageToken, getWebsocketToken, persistedCache = new odspCache_js_1.LocalPersistentCache(), hostPolicy = {}) {
        this.getStorageToken = getStorageToken;
        this.getWebsocketToken = getWebsocketToken;
        this.persistedCache = persistedCache;
        this.hostPolicy = hostPolicy;
        this.nonPersistentCache = new odspCache_js_1.NonPersistentCache();
        /**
         * The compatibility details of the ODSP Driver layer that is exposed to the Loader layer
         * for validating Loader-Driver compatibility.
         * @remarks This is for internal use only.
         * The type of this should be ILayerCompatDetails. However, ILayerCompatDetails is internal and this class
         * is currently marked as legacy alpha. So, using unknown here.
         */
        this.ILayerCompatDetails = odspLayerCompatState_js_1.odspDriverCompatDetailsForLoader;
        if (this.hostPolicy.isolateSocketCache === true) {
            // create the key to separate the socket reuse cache
            this.socketReferenceKeyPrefix = (0, uuid_1.v4)();
        }
        // Set enableRedeemFallback by default as true.
        this.hostPolicy.enableRedeemFallback = this.hostPolicy.enableRedeemFallback ?? true;
        this.hostPolicy.sessionOptions = {
            forceAccessTokenViaAuthorizationHeader: true,
            ...this.hostPolicy.sessionOptions,
        };
    }
    async createDocumentService(resolvedUrl, logger, clientIsSummarizer) {
        return this.createDocumentServiceCore(resolvedUrl, (0, odspUtils_js_1.createOdspLogger)(logger), undefined, clientIsSummarizer);
    }
    async createDocumentServiceCore(resolvedUrl, odspLogger, cacheAndTrackerArg, clientIsSummarizer) {
        const extLogger = (0, internal_3.createChildLogger)({ logger: odspLogger });
        const odspResolvedUrl = (0, odspUtils_js_1.getOdspResolvedUrl)(resolvedUrl);
        const resolvedUrlData = {
            siteUrl: odspResolvedUrl.siteUrl,
            driveId: odspResolvedUrl.driveId,
            itemId: odspResolvedUrl.itemId,
        };
        const cacheAndTracker = cacheAndTrackerArg ??
            (0, epochTracker_js_1.createOdspCacheAndTracker)(this.persistedCache, this.nonPersistentCache, {
                resolvedUrl: odspResolvedUrl,
                docId: odspResolvedUrl.hashedDocumentId,
                fileVersion: odspResolvedUrl.fileVersion,
            }, extLogger, clientIsSummarizer);
        const storageTokenFetcher = (0, odspUtils_js_1.toInstrumentedOdspStorageTokenFetcher)(extLogger, resolvedUrlData, this.getStorageToken);
        const webSocketTokenFetcher = this.getWebsocketToken === undefined
            ? undefined
            : async (options) => 
            // websocket expects a plain token
            (0, odspUtils_js_1.toInstrumentedOdspTokenFetcher)(extLogger, resolvedUrlData, this.getWebsocketToken, false /* throwOnNullToken */, true /* returnPlainToken */)(options, "GetWebsocketToken");
        return odspDocumentService_js_1.OdspDocumentService.create(resolvedUrl, storageTokenFetcher, webSocketTokenFetcher, extLogger, cacheAndTracker.cache, this.hostPolicy, cacheAndTracker.epochTracker, this.socketReferenceKeyPrefix, clientIsSummarizer);
    }
}
exports.OdspDocumentServiceFactoryCore = OdspDocumentServiceFactoryCore;
/**
 * Extract the sharing link kind from the resolved URL's query paramerters
 */
function getSharingLinkParams(hostPolicy, searchParams) {
    // extract request parameters for creation of sharing link (if provided) if the feature is enabled
    let createShareLinkParam;
    if (hostPolicy.enableSingleRequestForShareLinkWithCreate) {
        const createLinkScope = searchParams.get("createLinkScope");
        const createLinkRole = searchParams.get("createLinkRole");
        if (createLinkScope && internal_2.SharingLinkScope[createLinkScope]) {
            createShareLinkParam = {
                // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
                scope: internal_2.SharingLinkScope[createLinkScope],
                ...(createLinkRole && internal_2.SharingLinkRole[createLinkRole]
                    ? // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
                        { role: internal_2.SharingLinkRole[createLinkRole] }
                    : {}),
            };
        }
    }
    return createShareLinkParam;
}
//# sourceMappingURL=odspDocumentServiceFactoryCore.js.map